import { query } from '../config/db.js';
export async function upsertRating(req,res){ const storeId=Number(req.params.storeId); const rating=Number(req.body.rating); if(!Number.isInteger(rating)||rating<1||rating>5)return res.status(400).json({message:'Rating must be an integer from 1 to 5.'}); const store=await query('SELECT id FROM stores WHERE id=$1',[storeId]); if(!store.rowCount)return res.status(404).json({message:'Store not found.'}); const r=await query(`INSERT INTO ratings(user_id,store_id,rating) VALUES($1,$2,$3) ON CONFLICT(user_id,store_id) DO UPDATE SET rating=EXCLUDED.rating,updated_at=NOW() RETURNING *`,[req.user.id,storeId,rating]); res.json({message:'Rating saved successfully.',rating:r.rows[0]}); }
