import express from 'express'; import cors from 'cors'; import helmet from 'helmet'; import authRoutes from './routes/authRoutes.js'; import adminRoutes from './routes/adminRoutes.js'; import userRoutes from './routes/userRoutes.js'; import ratingRoutes from './routes/ratingRoutes.js'; import ownerRoutes from './routes/ownerRoutes.js'; import { authenticate, authorize } from './middleware/authMiddleware.js'; import { notFound, errorHandler } from './middleware/errorMiddleware.js';
const app=express(); app.use(helmet()); app.use(cors({origin:process.env.CLIENT_URL||'http://localhost:5173'})); app.use(express.json()); app.get('/api/health',(req,res)=>res.json({status:'ok'})); app.use('/api/auth',authRoutes); app.use('/api/admin',authenticate,authorize('ADMIN'),adminRoutes); app.use('/api/user',authenticate,authorize('USER'),userRoutes); app.use('/api/ratings',authenticate,authorize('USER'),ratingRoutes); app.use('/api/owner',authenticate,authorize('OWNER'),ownerRoutes); app.use(notFound); app.use(errorHandler); export default app;
