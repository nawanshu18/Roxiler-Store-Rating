import {useState} from 'react'; import {Link,useNavigate} from 'react-router-dom'; import {useAuth} from '../context/AuthContext.jsx';
export default function Register(){const [f,setF]=useState({name:'',email:'',address:'',password:''});const [error,setError]=useState('');const {register}=useAuth();const nav=useNavigate();const submit=async e=>{e.preventDefault();setError('');try{await register(f);nav('/user')}catch(e){setError(e.response?.data?.message||Object.values(e.response?.data?.errors||{})[0]||'Registration failed.')}};return <div className="auth-page"><div className="auth-card wide"><h1>Create your account</h1><p className="muted">Normal users can register here.</p>{error&&<div className="alert error">{error}</div>}<form onSubmit={submit}><label>Full name <span>20–60 chars</span><input required minLength="20" maxLength="60" value={f.name} onChange={e=>setF({...f,name:e.target.value})}/></label><label>Email<input type="email" required value={f.email} onChange={e=>setF({...f,email:e.target.value})}/></label><label>Address <span>max 400 chars</span><textarea required maxLength="400" value={f.address} onChange={e=>setF({...f,address:e.target.value})}/></label><label>Password <span>8–16, uppercase + special</span><input type="password" required minLength="8" maxLength="16" value={f.password} onChange={e=>setF({...f,password:e.target.value})}/></label><button className="primary">Create account</button></form><p className="switch">Already registered? <Link to="/login">Sign in</Link></p></div></div>}
