import {useEffect,useState} from 'react';import {Star} from 'lucide-react';import api from '../../services/api.js';
export default function Stores(){const [rows,setRows]=useState([]),[owners,setOwners]=useState([]),[show,setShow]=useState(false);const [q,setQ]=useState({name:'',email:'',address:''});const [f,setF]=useState({name:'',email:'',address:'',ownerId:''});const load=()=>api.get('/admin/stores',{params:q}).then(r=>setRows(r.data));useEffect(()=>{load();api.get('/admin/owners').then(r=>setOwners(r.data))},[]);const add=async e=>{e.preventDefault();try{await api.post('/admin/stores',f);setShow(false);setF({name:'',email:'',address:'',ownerId:''});load()}catch(e){alert(e.response?.data?.message||'Unable to add store')}};return <><div className="page-title"><div><h2>Stores</h2><p className="muted">Registered stores and their overall ratings.</p></div><button className="primary" onClick={()=>setShow(true)}>+ Add store</button></div><div className="filters"><input placeholder="Search name" value={q.name} onChange={e=>setQ({...q,name:e.target.value})}/><input placeholder="Email" value={q.email} onChange={e=>setQ({...q,email:e.target.value})}/><input placeholder="Address" value={q.address} onChange={e=>setQ({...q,address:e.target.value})}/><button onClick={load}>Apply</button></div><div className="panel table-wrap"><table><thead><tr><th><button className="th-btn" onClick={async()=>{const next=q.sortDir==='asc'?'desc':'asc';const r=await api.get('/admin/stores',{params:{...q,sortBy:'name',sortDir:next}});setRows(r.data);setQ({...q,sortBy:'name',sortDir:next})}}>Store ↕</button></th><th><button className="th-btn" onClick={async()=>{const next=q.sortDir==='asc'?'desc':'asc';const r=await api.get('/admin/stores',{params:{...q,sortBy:'email',sortDir:next}});setRows(r.data);setQ({...q,sortBy:'email',sortDir:next})}}>Email ↕</button></th><th>Address</th><th><button className="th-btn" onClick={async()=>{const next=q.sortDir==='asc'?'desc':'asc';const r=await api.get('/admin/stores',{params:{...q,sortBy:'rating',sortDir:next}});setRows(r.data);setQ({...q,sortBy:'rating',sortDir:next})}}>Rating ↕</button></th></tr></thead><tbody>{rows.map(r=><tr key={r.id}><td>{r.name}</td><td>{r.email}</td><td>{r.address}</td><td><span className="rating"><Star size={15} fill="currentColor"/>{r.rating}</span></td></tr>)}</tbody></table>{!rows.length&&<div className="empty">No stores found.</div>}</div>{show&&<div className="modal"><form className="modal-card" onSubmit={add}><h3>Add store</h3><input placeholder="Store name (20–60 chars)" minLength="20" maxLength="60" required value={f.name} onChange={e=>setF({...f,name:e.target.value})}/><input type="email" placeholder="Store email" required value={f.email} onChange={e=>setF({...f,email:e.target.value})}/><textarea placeholder="Address" maxLength="400" required value={f.address} onChange={e=>setF({...f,address:e.target.value})}/><select required value={f.ownerId} onChange={e=>setF({...f,ownerId:e.target.value})}><option value="">Select store owner</option>{owners.map(o=><option value={o.id} key={o.id}>{o.name} — {o.email}</option>)}</select><div className="actions"><button type="button" onClick={()=>setShow(false)}>Cancel</button><button className="primary">Create</button></div></form></div>}</>}
