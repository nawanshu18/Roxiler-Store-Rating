import {useEffect,useState} from 'react';import api from '../../services/api.js';
export default function Users(){const [rows,setRows]=useState([]);const [q,setQ]=useState({name:'',email:'',address:'',role:'',sortBy:'name',sortDir:'asc'});const [show,setShow]=useState(false);const [f,setF]=useState({name:'',email:'',password:'',address:'',role:'USER'});const load=()=>api.get('/admin/users',{params:q}).then(r=>setRows(r.data));useEffect(()=>{load()},[q.sortBy,q.sortDir,q.role]);const add=async e=>{e.preventDefault();try{await api.post('/admin/users',f);setShow(false);setF({name:'',email:'',password:'',address:'',role:'USER'});load()}catch(e){alert(e.response?.data?.message||'Unable to add user')}};return <><div className="page-title"><div><h2>Users</h2><p className="muted">Search, filter, sort and inspect users.</p></div><button className="primary" onClick={()=>setShow(true)}>+ Add user</button></div><div className="filters"><input placeholder="Search name" value={q.name} onChange={e=>setQ({...q,name:e.target.value})}/><input placeholder="Email" value={q.email} onChange={e=>setQ({...q,email:e.target.value})}/><input placeholder="Address" value={q.address} onChange={e=>setQ({...q,address:e.target.value})}/><select value={q.role} onChange={e=>setQ({...q,role:e.target.value})}><option value="">All roles</option><option value="ADMIN">Admin</option><option value="USER">Normal User</option></select><button onClick={load}>Apply</button></div><div className="panel table-wrap"><table><thead><tr><th><button className="th-btn" onClick={()=>setQ({...q,sortBy:'name',sortDir:q.sortDir==='asc'?'desc':'asc'})}>Name ↕</button></th><th><button className="th-btn" onClick={()=>setQ({...q,sortBy:'email',sortDir:q.sortDir==='asc'?'desc':'asc'})}>Email ↕</button></th><th><button className="th-btn" onClick={()=>setQ({...q,sortBy:'address',sortDir:q.sortDir==='asc'?'desc':'asc'})}>Address ↕</button></th><th>Role</th></tr></thead><tbody>{rows.map(r=><tr key={r.id}><td><button className="link-btn" onClick={async()=>{const d=await api.get(`/admin/users/${r.id}`);alert(`Name: ${d.data.name}\nEmail: ${d.data.email}\nAddress: ${d.data.address}\nRole: ${d.data.role}${d.data.rating!==null?'\nRating: '+d.data.rating:''}`)}}>{r.name}</button></td><td>{r.email}</td><td>{r.address}</td><td><span className="badge">{r.role}</span></td></tr>)}</tbody></table>{!rows.length&&<div className="empty">No users found.</div>}</div>{show&&<div className="modal"><form className="modal-card" onSubmit={add}><h3>Add user</h3><input placeholder="Name (20–60 chars)" minLength="20" maxLength="60" required value={f.name} onChange={e=>setF({...f,name:e.target.value})}/><input type="email" placeholder="Email" required value={f.email} onChange={e=>setF({...f,email:e.target.value})}/><input type="password" placeholder="Password" required minLength="8" maxLength="16" value={f.password} onChange={e=>setF({...f,password:e.target.value})}/><textarea placeholder="Address" maxLength="400" required value={f.address} onChange={e=>setF({...f,address:e.target.value})}/><select value={f.role} onChange={e=>setF({...f,role:e.target.value})}><option value="USER">Normal User</option><option value="ADMIN">Admin</option><option value="OWNER">Store Owner</option></select><div className="actions"><button type="button" onClick={()=>setShow(false)}>Cancel</button><button className="primary">Create</button></div></form></div>}</>}
