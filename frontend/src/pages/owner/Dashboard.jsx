import {useEffect,useState} from 'react';import {Star} from 'lucide-react';import api from '../../services/api.js';export default function Dashboard(){const [d,setD]=useState(null);useEffect(()=>{api.get('/owner/dashboard').then(r=>setD(r.data)).catch(e=>alert(e.response?.data?.message))},[]);return <><div className="page-title"><div><h2>Store owner dashboard</h2><p className="muted">Monitor feedback for your store.</p></div></div>{d&&<><div className="owner-hero"><div><span className="eyebrow">Your store</span><h2>{d.store.name}</h2><p>{d.store.address}</p></div><div className="big-rating"><Star size={28} fill="currentColor"/><strong>{d.averageRating}</strong><span>Average rating</span></div></div><div className="panel"><div className="panel-heading"><h3>Customers who rated your store</h3><span className="badge">{d.ratings.length} ratings</span></div><div className="table-wrap"><table><thead><tr><th>Name</th><th>Email</th><th>Rating</th><th>Updated</th></tr></thead><tbody>{d.ratings.map(r=><tr key={r.id}><td>{r.name}</td><td>{r.email}</td><td><span className="rating"><Star size={15} fill="currentColor"/>{r.rating}</span></td><td>{new Date(r.updated_at).toLocaleDateString()}</td></tr>)}</tbody></table>{!d.ratings.length&&<div className="empty">No ratings submitted yet.</div>}</div></div></>}</>}
